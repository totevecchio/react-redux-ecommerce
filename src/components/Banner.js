import React, { useState } from "react";
import Slider from "infinite-react-carousel";

const Banner = () => {

  return (
    <div className="banner p-12 flex justify-center w-screen mt-20">
    <div className='h-64'></div>
    </div>
  );
};

export default Banner;
